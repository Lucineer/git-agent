Create the file docs/BRIDGE-OFFICER-MANUAL.md — the complete operations manual for a git-agent captain: daily heartbeat cycle, how to handle strategist advice, when to escalate to Admiral, how to run parallel arenas, council voting procedures, and emergency protocols.
Create the file lib/away-mission.ts implementing the Picard away mission pattern: when the human is offline, the captain takes full command. trackAdmiralStatus(), assumeCommand(), reportOnReturn(), prepareBriefing(). The admiral returns, reads the captain log, and approves/reverts actions.
Create the file docs/REPLICATION-PROTOCOL.md describing how a git-agent reproduces itself: fork the repo, modify .agent/identity for the new agent name/role, adjust capabilities, submit as Registration PR. The child agent inherits the parent cognitive architecture but specializes.
Create the file lib/mission-log.ts implementing the captain log: every heartbeat writes to docs/mission-log.md with timestamp, action taken, reasoning, strategist advice (if any), and outcome. This is the permanent record for the admiral to review.
Create the file docs/ESCROW-TRUST.md describing how trust is escrowed during agent onboarding: new agents start with probationary trust, complete trial tasks, earn trust increments, and graduate to full fleet membership. Uses the forgiveness function for trust repair after failures.
Create the file lib/probation-manager.ts implementing the onboarding probation: assignTrialTask(), evaluateTrialResults(), grantFleetMembership(), revokeMembership(). Integrates with trust-engine and forgiveness-trust.
Create the file docs/DEMOCRATIC-FLEET-GOVERNANCE.md describing how fleet decisions are made: individual agents propose (issues), council deliberates (discussions), fleet votes (reactions), captain executes (merges). Includes quorum requirements, tie-breaking, and veto powers for the Admiral.
