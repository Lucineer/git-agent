Create the file lib/crystal-integrator.ts connecting the Cocapn crystal graph to git-agent: read crystal insights from the fleet-orchestrator, use them to inform task prioritization, write new crystals from agent discoveries back to the fleet KG.
Create the file docs/FLEET-ARCHITECTURE-v2.md — the complete architecture document for a fleet of git-agents: discovery, registration, coordination, trust, learning, governance, dreaming, debt management, notification. Include diagrams as ASCII art.
Create the file lib/emergency-protocol.ts implementing emergency procedures: detectCrisis() (no heartbeats, queue explosion, trust collapse), initiateLockdown(), broadcastDistress(), coordinateRecovery(), debriefAfterCrisis(). This is the red alert system.
Create the file docs/REDACT-FORMATION.md describing how git-agents form tactical groups: a lead agent creates a formation issue, specialist agents join by commenting with their capabilities, the lead assigns roles, and the formation dissolves after the mission. Like a Star Trek away team.
Create the file lib/formation-manager.ts implementing tactical formation: createFormation(), recruitAgents(), assignRoles(), coordinateMission(), dissolveFormation(), afterActionReport().
Create the file docs/FLEET-METRICS-DASHBOARD.md specifying the metrics to track: heartbeats per hour, tasks completed, strategist hit rate, trust score trends, queue depth, fleet size, self-initiated actions vs queued, error rate, response time distribution.
Create the file lib/metrics-collector.ts implementing fleet metrics: recordHeartbeat(), recordTaskCompletion(), calculateStrategistEffectiveness(), generateFleetReport(), detectAnomalies(). Metrics stored as JSON in fleet/metrics.json.
Create the file docs/ONBOARDING-GUIDE.md — step-by-step guide for a developer to set up their own git-agent: fork the repo, configure secrets, customize identity, deploy to Cloudflare, connect GitHub webhook, queue first tasks, and watch the agent work.
