Create the file lib/dead-reckoning-bridge.ts connecting the Dead Reckoning Engine to git-agent: compass-bearing maps to GitHub Issues, dead-reckoning maps to working branches, working-theory maps to PRs, ground-truth maps to merged PRs with tags, published maps to releases.
Create the file docs/MORPHIC-INTEGRITY.md describing how git-agents maintain Morphic Integrity: each agent has a signature cognitive pattern (its identity + model preferences + thinking style). When agents coordinate, their patterns should complement, not duplicate. The council detects integrity violations.
Create the file lib/integrity-checker.ts implementing Morphic Integrity validation: computeSignature(), detectDuplication(), suggestSpecialization(), assessFleetDiversity(). Ensures the fleet has complementary agents, not redundant ones.
Create the file docs/SOFTWARE-SOMNOLOGY.md describing how git-agents dream: during off-hours (low-traffic periods), the agent runs batch heartbeats in think-only mode — no file writes, just reasoning about accumulated experience. The strategist guides the dream. Morning = insights from overnight processing.
Create the file lib/dream-engine.ts implementing the somnology system: enterDreamMode(), processExperience(), generateInsights(), wakeWithReport(). Integrates with the strategist for guided dreaming and produces a morning brief for the admiral.
Create the file docs/DEVELOPMENTAL-DEBT.md describing how git-agents accumulate and manage technical debt: trackShortcuts(), assessDebtImpact(), planPaydown(), communicateTradeoffs(). Debt is tracked in .agent/debt and included in vessel status reports.
Create the file lib/debt-tracker.ts implementing developmental debt management: recordShortcut(), calculateDebtScore(), prioritizePaydown(), generateDebtReport(). Debt items are tracked as GitHub issues with label "tech-debt".
Create a GitHub issue titled "Senior Council Motion: Adopt Dead Reckoning as Standard Operating Procedure" proposing that all fleet agents use the Dead Reckoning pipeline for complex multi-step tasks. Label it "council" and include arguments for and against.
