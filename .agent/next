Create the file lib/learning-engine.ts implementing cross-agent learning: extractLessons() reads .agent/done from other repos, shareInsight() creates a "lesson" file in a shared location, buildOnPriorWork() checks existing lessons before starting new work, detectDiminishingReturns() flags when an agent is repeating work.
Create the file docs/REGISTRATION-PR-PROTOCOL.md describing how a new git-agent joins the fleet: it forks the fleet-manifest repo, adds its .agent/identity to the roster, and submits a PR. Existing agents review the PR, test the new agent by assigning a trial task, and vote to accept or reject.
Create the file lib/parallel-arena.ts implementing Iron-Sharpens-Iron: when two approaches conflict, create two branches, each running the competing code, compare results via automated tests, merge the winner. Functions: createArena(), runCompeting(), evaluateResults(), mergeWinner(), archiveLoser().
Create the file docs/CODESPACE-INTEGRATION.md describing how git-agent appears as a panel in GitHub Codespaces: the developer opens codespaces, sees the agent working in real-time on its branch, can review PRs, approve merges, or override the captain. Like Riker putting the crew on the viewscreen.
Create the file lib/task-prioritizer.ts implementing intelligent task prioritization: scoreTask() considers urgency, strategic alignment, dependencies, and estimated effort. prioritizeQueue() reorders .agent/next. detectStalledTasks() flags tasks that have been queued too long.
Create the file docs/NOTIFICATION-ARCHITECTURE.md specifying the full notification system: vessel status levels, escalation rules, human attention request format with priority/ETA/options, fleet-wide alerts, and integration with Telegram/Discord/Slack.
Create a GitHub issue titled "Registration PR: Agent Ember" proposing a second git-agent named Ember that specializes in code generation and testing. Include its .agent/identity, proposed capabilities, and trial task assignment.
Create the file lib/git-cognitive-map.ts implementing the complete git-to-cognition mapping: gitStatus=selfAwareness, gitCommit=memoryConsolidation, gitBranch=hypotheticalReasoning, gitMerge=synthesis, gitCherryPick=insightExtraction, gitRebase=rewritingHistory, gitBlame=attribution, gitDiff=comparisonThinking, gitStash=suspendWorkingMemory, gitTag=graduation, gitFork=reproduction, gitPR=proposal, gitIssue=question. Each as a typed function that wraps the git operation with cognitive metadata.
