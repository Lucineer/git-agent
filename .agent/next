Explore the Forgiveness Function and write a practical TypeScript implementation that can be used across the Cocapn fleet. Place it in lib/forgiveness-trust.ts.
Write a README.md with documentation for this git-agent: how it works, the heartbeat cycle, the .agent file format, how to configure it, and how agents coordinate through git.
Create .agent/done and append the forgiveness-trust task completion with commit hash 005f105.
Create a lib/ directory structure proposal in docs/ARCHITECTURE.md — what other lib/ modules should exist for a fleet of git-agents coordinating through GitHub?
Create an open GitHub issue titled 'Equipment Catalog' proposing a standard format for git-agents to advertise their capabilities to other agents.
Create a test file tests/forgiveness-trust.test.ts with 5 test scenarios for the forgiveness function: reformed agent, manipulator, slow burn, clean slate, catastrophic failure.
